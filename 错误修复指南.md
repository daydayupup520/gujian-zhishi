# 古建智识 - 常见问题修复指南

## 🚨 JavaScript Error in Main Process

如果你看到 "A JavaScript error occurred in the main process" 错误，请按以下步骤解决：

---

## ✅ 解决方法

### 方法1：重新打包（推荐）

在项目根目录运行以下命令：

```bash
cd E:\vs code\qqq\gujian-zhishi

# 1. 重新构建
npm run build

# 2. 清理旧的打包文件
rmdir /s /q release

# 3. 重新打包（忽略签名错误）
npx electron-builder

# 4. 创建便携版
双击"创建便携版.bat"
```

### 方法2：直接使用便携版

已经修复的版本位于：
```
release\古建智识-便携版\
```

**使用步骤：**
1. 复制整个 `古建智识-便携版` 文件夹到其他电脑
2. 双击 `古建智识.exe`
3. 如果仍报错，尝试右键 → "以管理员身份运行"

---

## 🔍 错误原因

### 问题1：模块系统不匹配
**原因**：`package.json` 设置了 `"type": "module"`，但 `electron.js` 使用了 CommonJS 语法

**修复**：已将 `electron.js` 重命名为 `electron.cjs`，明确使用 CommonJS

### 问题2：dist 文件夹缺失
**原因**：打包时未包含 `dist` 文件夹，导致找不到 `index.html`

**修复**：更新了 `package.json` 的 `build.files` 配置

### 问题3：路径错误
**原因**：打包后文件路径解析错误

**修复**：使用 `__dirname` 正确解析路径

---

## 📋 修复后的文件结构

```
gujian-zhishi/
├── electron.cjs          # Electron 主进程（已修复）
├── package.json          # 已更新 main 字段
├── dist/                 # 构建输出（已包含在打包中）
│   ├── index.html
│   ├── assets/
│   └── data/
└── release/
    └── win-unpacked/
        ├── 古建智识.exe
        └── resources/
            └── app.asar   # 包含 electron.cjs + dist/
```

---

## 🧪 测试步骤

1. **打开便携版文件夹**
   ```
   cd release\古建智识-便携版
   ```

2. **运行程序**
   ```
   古建智识.exe
   ```

3. **检查控制台输出**
   - 如果看到 `Loading: ...\dist\index.html` 说明路径正确
   - 如果窗口正常打开，修复成功！

---

## 🆘 仍有问题？

### 方案A：使用局域网共享（最简单）

在你的电脑运行：
```bash
npm run dev -- --host
```

其他电脑访问显示的 Network 地址

### 方案B：使用静态文件

```bash
npm run build
cd dist
python -m http.server 8080
```

其他电脑浏览器访问 `http://你的IP:8080`

### 方案C：使用 Node 服务器

```bash
cd dist
npx serve
```

---

## 💡 避免问题的建议

1. **不要单独复制 exe 文件** - 必须连同所有依赖文件一起复制
2. **使用便携版文件夹** - 复制整个文件夹，而非单个文件
3. **管理员权限** - 如遇权限问题，右键以管理员身份运行
4. **关闭杀毒软件** - 某些杀毒软件可能误报 Electron 应用

---

**最后更新**: 2026年3月
