# 古建智识 - 部署指南

> 让其他电脑运行项目的3种方案

---

## 🚀 方案对比

| 方案 | 难度 | 适用场景 | 特点 |
|-----|------|---------|------|
| **方案一：局域网共享** | ⭐ 最简单 | 临时演示 | 即时可用，无需安装 |
| **方案二：桌面应用** | ⭐⭐ 简单 | 给客户/评委 | 独立运行，双击即用 |
| **方案三：静态文件** | ⭐⭐⭐ 中等 | 多平台使用 | 跨平台，需要服务器 |

---

## 方案一：局域网共享（推荐临时使用）

在你的电脑运行，其他电脑通过浏览器访问。

### 步骤

```bash
# 1. 进入项目目录
cd E:\vs code\qqq\gujian-zhishi

# 2. 启动服务器（带网络访问）
npm run dev -- --host
```

### 其他电脑访问

1. 确保所有电脑连接**同一WiFi/局域网**
2. 查看终端显示的 **Network** 地址：
   ```
   Network: http://192.168.124.2:5173
   ```
3. 其他电脑浏览器输入该地址即可访问

### ✅ 优点 & ❌ 缺点

- ✅ 无需安装任何软件
- ✅ 即时可用
- ❌ 需要你电脑保持开机
- ❌ 需要同一网络环境

---

## 方案二：桌面应用（推荐给客户/评委）

打包成Windows安装程序，双击安装即可使用。

### 步骤

```bash
# 1. 进入项目目录
cd E:\vs code\qqq\gujian-zhishi

# 2. 安装依赖（首次）
npm install

# 3. 打包桌面应用
npm run dist
```

### 生成文件

打包完成后在 `release/` 目录生成：
```
release/
├── win-unpacked/              # 便携版文件夹
│   ├── 古建智识.exe          # 双击直接运行
│   ├── 启动程序.bat          # 带中文提示的启动脚本
│   └── 使用说明.txt          # 使用文档
└── builder-debug.yml
```

### 给其他电脑使用

**方法A - 便携版（推荐）**：
1. 复制整个 `win-unpacked` 文件夹到U盘（约200MB）
2. 粘贴到其他电脑任意位置
3. 双击 `古建智识.exe` 即可运行

**方法B - 安装包**：
> 注：由于签名权限问题，可能需要以管理员身份运行打包命令

### ✅ 优点 & ❌ 缺点

- ✅ 无需Node.js环境
- ✅ 无需联网（除AI功能）
- ✅ 双击即用
- ❌ 文件较大（约200MB）
- ❌ 仅支持Windows

---

## 方案三：静态文件（跨平台）

打包为静态网页文件，通过HTTP服务器访问。

### 步骤

```bash
# 1. 构建静态文件
cd E:\vs code\qqq\gujian-zhishi
npm run build

# 2. 生成 dist/ 文件夹
```

### 在其他电脑运行

**方式A - Python（推荐，最简单）**：
```bash
# 在目标电脑
cd dist
python -m http.server 8080

# 浏览器访问 http://localhost:8080
```

**方式B - Node.js**：
```bash
cd dist
npx serve

# 自动打开浏览器
```

**方式C - VS Code**：
1. 在VS Code中打开 `dist` 文件夹
2. 安装 **Live Server** 插件
3. 右键 `index.html` → "Open with Live Server"

### ✅ 优点 & ❌ 缺点

- ✅ 跨平台（Windows/Mac/Linux）
- ✅ 文件较小（约10MB）
- ❌ 需要Python或Node.js环境
- ❌ 需要启动服务器

---

## 📋 快速选择指南

| 你的需求 | 推荐方案 |
|---------|---------|
| 临时给同学演示 | **方案一**（局域网共享） |
| 给客户/评委展示 | **方案二**（桌面应用） |
| 需要在Mac上运行 | **方案三**（静态文件） |
| 需要离线使用 | **方案二**（桌面应用） |
| 需要最小文件 | **方案三**（静态文件） |

---

## ⚠️ 重要提示

### 1. API密钥配置
AI识别功能需要联网并配置智谱清言API密钥：
- 在源项目 `.env` 文件中配置
- 或在目标电脑创建 `.env` 文件

### 2. 图片资源
确保以下文件夹已正确包含：
- `dist/data/images/` - 建筑图片（20张）
- `dist/data/knowledge/` - 知识文档（10篇）

### 3. 防火墙设置
方案一（局域网共享）可能需要：
- 关闭Windows防火墙，或
- 开放端口 5173

---

## 🔧 常见问题

**Q: 双击exe没反应？**
> A: 确保 `win-unpacked` 文件夹内的所有文件都在同一目录，不要单独复制exe

**Q: 浏览器打开显示空白？**
> A: 不能直接双击打开HTML文件，必须使用HTTP服务器（Python或Node）

**Q: 局域网内其他电脑访问不了？**
> A: 检查防火墙设置，确保端口5173已开放

**Q: AI识别功能不能用？**
> A: 需要联网，并确保API密钥已正确配置

---

## 📦 文件大小对比

| 方案 | 大小 | 说明 |
|-----|------|------|
| 源码+依赖 | ~500MB | 包含 node_modules |
| 桌面应用 | ~200MB | Electron运行时 |
| 静态文件 | ~10MB | 仅构建产物 |

---

**最后更新**: 2026年3月
