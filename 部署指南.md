# 古建智识 - 部署指南

## 目录

1. [问题诊断](#一问题诊断)
2. [国内服务器部署方案](#二国内服务器部署方案)
3. [Docker 一键部署](#三docker-一键部署)
4. [静态托管部署](#四静态托管部署)
5. [跨电脑运行解决方案](#五跨电脑运行解决方案)
6. [故障排查](#六故障排查)

---

## 一、问题诊断

### 1.1 为什么别的电脑打不开？

**最常见原因**:
1. **缺少 API Key** - `.env.local` 文件未被 Git 提交，新电脑缺少智谱清言 API Key
2. **依赖未安装** - 未运行 `npm install`
3. **Node.js 版本不匹配** - 需要 Node.js >= 18
4. **网络问题** - 无法访问智谱清言 API

### 1.2 快速检查清单

在新电脑上按顺序执行：

```bash
# 1. 检查 Node.js 版本
node -v  # 需要 v18.0.0 或更高

# 2. 安装依赖
npm install

# 3. 创建环境配置文件
copy .env.example .env.local
# 然后编辑 .env.local，填入你的 API Key

# 4. 启动开发服务器
npm run dev
```

---

## 二、国内服务器部署方案

### 方案一：阿里云 ECS 部署（推荐）

#### 步骤 1：购买服务器

- **推荐配置**: 2核4G，带宽 3-5M
- **系统**: CentOS 8 / Ubuntu 22.04
- **地域**: 选择离你用户最近的地域

#### 步骤 2：环境准备

```bash
# 连接服务器
ssh root@你的服务器IP

# 安装 Node.js 18
curl -fsSL https://rpm.nodesource.com/setup_18.x | bash -
yum install -y nodejs

# 或 Ubuntu 系统
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 安装 Nginx
yum install -y nginx
# 或 Ubuntu: apt-get install -y nginx

# 安装 PM2（进程管理）
npm install -g pm2
```

#### 步骤 3：部署项目

```bash
# 创建项目目录
mkdir -p /var/www/gujian-zhishi
cd /var/www/gujian-zhishi

# 克隆代码（或上传代码）
git clone https://github.com/daydayupup520/gujian-zhishi.git .

# 安装依赖
npm install

# 创建环境配置
cat > .env.local << 'EOF'
VITE_ZHIPU_API_KEY=你的智谱清言API Key
EOF

# 构建生产版本
npm run build
```

#### 步骤 4：Nginx 配置

```bash
cat > /etc/nginx/conf.d/gujian-zhishi.conf << 'EOF'
server {
    listen 80;
    server_name _;  # 或你的域名
    
    root /var/www/gujian-zhishi/dist;
    index index.html;
    
    # 开启 gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    
    # 缓存静态资源
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # 前端路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API 代理（解决跨域）
    location /api/ {
        proxy_pass https://open.bigmodel.cn/;
        proxy_set_header Host open.bigmodel.cn;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
EOF

# 测试配置并重启 Nginx
nginx -t
systemctl restart nginx
```

#### 步骤 5：配置防火墙

```bash
# 开放 80 端口
firewall-cmd --permanent --add-service=http
firewall-cmd --reload

# 或阿里云安全组中开放 80 端口
```

#### 步骤 6：访问

浏览器访问：`http://你的服务器IP`

---

### 方案二：腾讯云轻量应用服务器

腾讯云轻量服务器更适合小型项目，性价比更高。

#### 一键部署脚本

```bash
#!/bin/bash
# deploy.sh - 一键部署脚本

set -e

echo "🚀 开始部署古建智识..."

# 更新系统
apt-get update && apt-get upgrade -y

# 安装 Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs nginx git

# 安装 PM2
npm install -g pm2

# 创建目录
mkdir -p /var/www
cd /var/www

# 克隆项目
if [ -d "gujian-zhishi" ]; then
    cd gujian-zhishi
    git pull
else
    git clone https://github.com/daydayupup520/gujian-zhishi.git
    cd gujian-zhishi
fi

# 安装依赖
npm install

# 提示输入 API Key
echo "请输入智谱清言 API Key:"
read API_KEY

# 创建环境文件
echo "VITE_ZHIPU_API_KEY=$API_KEY" > .env.local

# 构建
npm run build

# 复制到 Nginx 目录
cp -r dist/* /var/www/html/

# Nginx 配置
cat > /etc/nginx/sites-available/gujian-zhishi << 'EOF'
server {
    listen 80 default_server;
    root /var/www/html;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    gzip on;
    gzip_types text/plain text/css application/json application/javascript;
}
EOF

ln -sf /etc/nginx/sites-available/gujian-zhishi /etc/nginx/sites-enabled/default

# 重启 Nginx
systemctl restart nginx

echo "✅ 部署完成！访问 http://$(curl -s ip.sb)"
```

使用方法：
```bash
# 在服务器上执行
wget https://你的脚本地址/deploy.sh
chmod +x deploy.sh
./deploy.sh
```

---

## 三、Docker 一键部署

### 3.1 使用 Docker 部署（最简单）

#### 创建 Dockerfile

在项目根目录创建 `Dockerfile`：

```dockerfile
# 构建阶段
FROM node:18-alpine AS builder

WORKDIR /app

# 复制依赖文件
COPY package*.json ./
RUN npm install

# 复制源码
COPY . .

# 构建时传入 API Key（或使用默认值）
ARG VITE_ZHIPU_API_KEY=c5cc6b4d1afc40f1b73106ae07e95cd5.ug0fjCyrxXSdv9jm
ENV VITE_ZHIPU_API_KEY=$VITE_ZHIPU_API_KEY

# 构建
RUN npm run build

# 运行阶段
FROM nginx:alpine

# 复制构建产物
COPY --from=builder /app/dist /usr/share/nginx/html

# 复制 Nginx 配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 健康检查
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
  CMD curl -f http://localhost/ || exit 1

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

#### 创建 nginx.conf

```nginx
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # 开启 gzip
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # 缓存控制
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # 前端路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
```

#### 创建 docker-compose.yml

```yaml
version: '3.8'

services:
  gujian-zhishi:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_ZHIPU_API_KEY=${VITE_ZHIPU_API_KEY:-c5cc6b4d1afc40f1b73106ae07e95cd5.ug0fjCyrxXSdv9jm}
    container_name: gujian-zhishi
    ports:
      - "80:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3
```

#### 部署步骤

```bash
# 1. 进入项目目录
cd gujian-zhishi

# 2. 创建 .env 文件（可选，如果不创建会使用默认Key）
echo "VITE_ZHIPU_API_KEY=你的API Key" > .env

# 3. 构建并运行
docker-compose up -d

# 4. 查看日志
docker-compose logs -f

# 5. 停止
docker-compose down
```

### 3.2 上传到 Docker Hub（方便分享）

```bash
# 构建镜像
docker build -t yourname/gujian-zhishi:latest .

# 登录 Docker Hub
docker login

# 推送镜像
docker push yourname/gujian-zhishi:latest

# 别人使用时只需
docker run -d -p 80:80 yourname/gujian-zhishi:latest
```

---

## 四、静态托管部署

### 方案三：又拍云/七牛云对象存储

适合纯前端项目，成本最低。

#### 又拍云部署步骤

1. **注册又拍云账号** → 创建云存储服务
2. **绑定域名**（可选，使用测试域名也可以）
3. **上传文件**：

```bash
# 安装又拍云上传工具
npm install -g upyun-cli

# 登录
upyun login

# 构建项目
npm run build

# 上传到云存储
upyun upload dist/ / -s 你的服务名
```

4. **开启静态页面**：在又拍云控制台开启 "静态页面" 功能
5. **访问**：使用分配的测试域名或绑定域名访问

### 方案四：Vercel/Netlify 国内镜像

#### 使用 Vercel（需要代理）

```bash
# 安装 Vercel CLI
npm i -g vercel

# 登录
vercel login

# 部署
cd gujian-zhishi
vercel --prod
```

#### 使用 4EVERLAND（国内可访问）

4EVERLAND 是去中心化部署平台，国内可访问。

1. 访问 https://www.4everland.org/
2. 用 GitHub 登录
3. 导入项目
4. 自动部署

### 方案五：GitHub Pages + CDN 加速

```bash
# 安装 gh-pages
npm install -D gh-pages

# package.json 添加脚本
{
  "scripts": {
    "deploy": "npm run build && gh-pages -d dist"
  }
}

# 执行部署
npm run deploy
```

然后使用 **jsDelivr** CDN 加速：
`https://cdn.jsdelivr.net/gh/你的用户名/仓库名@分支名/`

---

## 五、跨电脑运行解决方案

### 5.1 完整迁移步骤

#### 方法 A：完整压缩包（最简单）

```bash
# 在原电脑上
cd gujian-zhishi

# 删除 node_modules（减少体积）
rm -rf node_modules

# 压缩项目
tar -czvf gujian-zhishi.tar.gz gujian-zhishi/

# 或 Windows 上右键发送到压缩文件
```

然后复制到新电脑：
```bash
# 解压
tar -xzvf gujian-zhishi.tar.gz

# 进入目录
cd gujian-zhishi

# 安装依赖
npm install

# 创建环境文件
copy .env.example .env.local
# 编辑 .env.local，填入 API Key

# 运行
npm run dev
```

#### 方法 B：Git 克隆（推荐）

```bash
# 1. 克隆项目
git clone https://github.com/daydayupup520/gujian-zhishi.git

# 2. 进入目录
cd gujian-zhishi

# 3. 安装依赖
npm install

# 4. 创建环境配置
copy .env.example .env.local
# 编辑 .env.local，添加：
# VITE_ZHIPU_API_KEY=你的API Key

# 5. 运行
npm run dev
```

**如何获取 API Key**：
1. 访问 https://open.bigmodel.cn/
2. 注册/登录
3. 创建 API Key
4. 免费额度：新用户有 100万 Tokens 免费额度

### 5.2 制作绿色免安装版

创建一个包含所有依赖的完整包：

```bash
# Windows 批处理脚本：打包.bat
cd gujian-zhishi

# 删除不必要的文件
rd /s /q .git
rd /s /q node_modules
rd /s /q e2e
rd /s /q deliverables

# 创建启动脚本
echo @echo off > 启动项目.bat
echo echo 正在安装依赖... >> 启动项目.bat
echo call npm install >> 启动项目.bat
echo echo 启动开发服务器... >> 启动项目.bat
echo call npm run dev >> 启动项目.bat
echo pause >> 启动项目.bat

# 创建说明文件
echo 使用说明：>> 使用说明.txt
echo 1. 确保已安装 Node.js 18+ (https://nodejs.org/) >> 使用说明.txt
echo 2. 编辑 .env.local 文件，填入你的智谱清言 API Key >> 使用说明.txt
echo    (从 https://open.bigmodel.cn/ 获取) >> 使用说明.txt
echo 3. 双击"启动项目.bat" >> 使用说明.txt
echo 4. 浏览器访问 http://localhost:5173 >> 使用说明.txt
```

### 5.3 嵌入 API Key（方便但注意保密）

修改代码，将 API Key 作为默认值嵌入：

```typescript
// src/lib/ai/recognition.ts
// 修改第12行
const API_KEY = import.meta.env.VITE_ZHIPU_API_KEY || '你的API Key';
```

这样即使 `.env.local` 不存在也能运行，但要注意：
- ⚠️ 不要将包含真实 Key 的代码提交到公共仓库
- ⚠️ 仅用于演示或内部使用

---

## 六、故障排查

### 6.1 常见问题

#### Q1: 运行 `npm install` 报错

**原因**: Node.js 版本过低或网络问题

**解决**:
```bash
# 检查版本
node -v  # 需要 >= 18

# 如果版本不够，升级 Node.js
# Windows: 官网下载安装包
# Mac: brew upgrade node
# Linux: 使用 nvm

# 如果是网络问题，使用淘宝镜像
npm config set registry https://registry.npmmirror.com
npm install
```

#### Q2: 启动后 AI 识别失败

**原因**: 缺少 API Key 或 Key 无效

**解决**:
1. 检查 `.env.local` 文件是否存在
2. 检查 API Key 是否正确
3. 在浏览器控制台查看具体错误

#### Q3: 构建失败

**解决**:
```bash
# 清理缓存
rm -rf node_modules
rm package-lock.json
npm install
npm run build
```

#### Q4: 部署后页面空白

**原因**: 资源路径问题

**解决**:
检查 `vite.config.ts` 中的 `base` 配置：
```typescript
export default defineConfig({
  base: './',  // 使用相对路径
  // ...
})
```

#### Q5: 智谱 API 访问超时

**原因**: 服务器网络问题

**解决**:
1. 检查服务器能否访问外网
2. 配置代理：
```bash
# 在服务器上测试
curl https://open.bigmodel.cn/api/paas/v4/models

# 如果不通，配置代理
export https_proxy=http://代理地址:端口
```

### 6.2 一键诊断脚本

创建 `check.sh`：

```bash
#!/bin/bash

echo "🔍 古建智识环境诊断..."
echo ""

# 检查 Node.js
echo "1. 检查 Node.js..."
if command -v node &> /dev/null; then
    NODE_VERSION=$(node -v)
    echo "   ✅ Node.js 版本: $NODE_VERSION"
    
    # 检查版本是否 >= 18
    NODE_MAJOR=$(node -v | cut -d'v' -f2 | cut -d'.' -f1)
    if [ "$NODE_MAJOR" -ge 18 ]; then
        echo "   ✅ 版本符合要求"
    else
        echo "   ❌ 版本过低，需要 >= 18"
    fi
else
    echo "   ❌ Node.js 未安装"
fi

echo ""
echo "2. 检查环境文件..."
if [ -f ".env.local" ]; then
    echo "   ✅ .env.local 存在"
    if grep -q "VITE_ZHIPU_API_KEY" .env.local; then
        echo "   ✅ API Key 已配置"
    else
        echo "   ❌ API Key 未配置"
    fi
else
    echo "   ❌ .env.local 不存在"
    echo "   💡 请运行: copy .env.example .env.local"
fi

echo ""
echo "3. 检查依赖..."
if [ -d "node_modules" ]; then
    echo "   ✅ node_modules 存在"
    MODULE_COUNT=$(ls node_modules | wc -l)
    echo "   📦 已安装 $MODULE_COUNT 个包"
else
    echo "   ❌ node_modules 不存在"
    echo "   💡 请运行: npm install"
fi

echo ""
echo "4. 检查构建产物..."
if [ -d "dist" ]; then
    echo "   ✅ dist 目录存在"
    DIST_SIZE=$(du -sh dist | cut -f1)
    echo "   📦 大小: $DIST_SIZE"
else
    echo "   ⚠️ dist 目录不存在（需要构建）"
fi

echo ""
echo "5. 网络连通性测试..."
if curl -s --max-time 5 https://open.bigmodel.cn > /dev/null; then
    echo "   ✅ 可以访问智谱 API"
else
    echo "   ⚠️ 无法访问智谱 API（检查网络或代理）"
fi

echo ""
echo "诊断完成！"
```

使用方法：
```bash
chmod +x check.sh
./check.sh
```

---

## 七、快速部署清单

### 最快部署路径（5分钟上线）

**方案：阿里云 ECS + Docker**

1. **购买服务器**（2核4G，3年特惠约 600元）
2. **SSH 连接服务器**
3. **一键安装**：

```bash
# 安装 Docker
curl -fsSL https://get.docker.com | sh

# 运行项目（使用预构建镜像）
docker run -d \
  --name gujian-zhishi \
  -p 80:80 \
  -e VITE_ZHIPU_API_KEY=你的API Key \
  --restart unless-stopped \
  你的镜像地址/gujian-zhishi:latest

# 完成！访问服务器 IP 即可
```

---

## 联系方式

如有部署问题，请检查：
1. 本文档的故障排查章节
2. 项目 GitHub Issues
3. 智谱清言官方文档: https://open.bigmodel.cn/dev/howuse/model

---

<p align="center">
  祝部署顺利！🏛️
</p>
