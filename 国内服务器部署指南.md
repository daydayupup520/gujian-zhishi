# 古建智识 - 国内服务器部署指南

## 🚀 快速开始（推荐方案）

### 方式一：一键脚本部署（最简单，5分钟完成）

**适用于**: 阿里云、腾讯云、华为云等国内云服务器

#### 1. 购买服务器

- **推荐配置**: 2核4G，带宽 3-5M
- **系统**: Ubuntu 22.04 LTS（推荐）或 CentOS 8
- **价格**: 约 600-800元/年（新用户更便宜）

#### 2. 连接服务器

```bash
# Windows 使用 PowerShell 或 Git Bash
ssh root@你的服务器IP

# 或使用密钥
ssh -i 密钥文件 root@你的服务器IP
```

#### 3. 运行一键部署脚本

```bash
# 下载部署脚本
curl -fsSL https://raw.githubusercontent.com/daydayupup520/gujian-zhishi/master/deploy.sh -o deploy.sh

# 或使用国内加速地址
curl -fsSL https://ghproxy.com/https://raw.githubusercontent.com/daydayupup520/gujian-zhishi/master/deploy.sh -o deploy.sh

# 赋予执行权限
chmod +x deploy.sh

# 运行脚本
./deploy.sh
```

脚本会自动完成：
- ✅ 安装 Docker 和 Docker Compose
- ✅ 下载项目代码
- ✅ 构建 Docker 镜像
- ✅ 启动服务
- ✅ 配置防火墙

#### 4. 访问网站

部署完成后，浏览器直接访问服务器 IP 即可：
```
http://你的服务器IP
```

---

### 方式二：手动 Docker 部署

如果你更喜欢手动控制每一步：

#### 1. 安装 Docker

```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com | bash -s docker --mirror Aliyun
sudo systemctl start docker
sudo systemctl enable docker

# 配置国内镜像加速
sudo mkdir -p /etc/docker
sudo tee /etc/docker/daemon.json <<-'EOF'
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ]
}
EOF
sudo systemctl restart docker
```

#### 2. 下载项目

```bash
# 创建目录
mkdir -p /opt/gujian-zhishi
cd /opt/gujian-zhishi

# 方式 A：Git 克隆
git clone https://github.com/daydayupup520/gujian-zhishi.git .

# 方式 B：上传代码（如果 Git 慢）
# 先在本地压缩项目，上传到服务器后解压
```

#### 3. 构建并运行

```bash
# 构建镜像（约5-10分钟）
docker-compose build

# 启动服务
docker-compose up -d

# 查看状态
docker-compose ps

# 查看日志
docker-compose logs -f
```

#### 4. 访问测试

```bash
# 获取服务器 IP
curl ip.sb

# 浏览器访问 http://服务器IP
```

---

### 方式三：静态文件部署（无需 Node.js）

如果你已经有构建好的 `dist` 文件夹：

#### 1. 在本地构建

```bash
# 在项目根目录
npm run build

# 会生成 dist 文件夹
```

#### 2. 上传到服务器

```bash
# 使用 scp 上传
scp -r dist root@你的服务器IP:/var/www/html/

# 或使用 rsync
rsync -avz dist/ root@你的服务器IP:/var/www/html/
```

#### 3. 安装 Nginx

```bash
# Ubuntu
sudo apt-get update
sudo apt-get install -y nginx

# CentOS
sudo yum install -y nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

#### 4. 配置 Nginx

```bash
sudo tee /etc/nginx/sites-available/gujian-zhishi << 'EOF'
server {
    listen 80;
    server_name _;
    
    root /var/www/html;
    index index.html;
    
    # 开启 gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript;
    
    # 前端路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 缓存静态资源
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
EOF

# 启用配置
sudo ln -s /etc/nginx/sites-available/gujian-zhishi /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

---

## 🌐 国内云平台推荐

### 阿里云 ECS

**优势**: 稳定、生态完善
**推荐配置**: 
- 2核4G，3M带宽
- 高效云盘 40GB
- 价格: ~700元/年

**部署步骤**:
1. 购买后获取公网 IP
2. 安全组开放 80 端口
3. 使用一键脚本部署

### 腾讯云轻量应用服务器

**优势**: 性价比高，新用户优惠大
**推荐配置**:
- 2核4G，5M带宽（轻量）
- 价格: ~200元/年（新用户）

**部署步骤**:
同阿里云，防火墙在控制台配置

### 华为云耀云服务器

**优势**: 企业级稳定
**推荐配置**:
- 2核4G，3M带宽
- 价格: ~600元/年

---

## 🔧 常见问题

### Q1: 部署后访问显示 404

**原因**: Nginx 未正确配置前端路由

**解决**:
```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

### Q2: 智谱 API 访问超时

**原因**: 服务器无法访问外网

**解决**:
```bash
# 测试网络连通性
curl https://open.bigmodel.cn

# 如果失败，配置代理或检查安全组出站规则
```

### Q3: Docker 构建很慢

**原因**: 使用国外镜像源

**解决**:
```bash
# 已在一键脚本中配置，手动配置如下：
cat > /etc/docker/daemon.json << 'EOF'
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ]
}
EOF
systemctl restart docker
```

### Q4: 如何更新项目？

```bash
cd /opt/gujian-zhishi

# 拉取最新代码
git pull

# 重新构建并启动
docker-compose down
docker-compose up -d --build

# 或者只更新静态文件
npm run build
docker cp dist gujian-zhishi:/usr/share/nginx/html/
```

### Q5: 如何绑定域名？

1. **域名解析**: 添加 A 记录指向服务器 IP
2. **修改 Nginx 配置**:
```nginx
server {
    listen 80;
    server_name 你的域名.com www.你的域名.com;
    # ...
}
```
3. **申请 SSL 证书**（推荐）：
```bash
# 安装 certbot
apt-get install certbot python3-certbot-nginx

# 自动配置 HTTPS
certbot --nginx -d 你的域名.com
```

---

## 📁 项目文件说明

部署后生成的重要文件：

```
/opt/gujian-zhishi/
├── docker-compose.yml    # Docker 编排文件
├── Dockerfile           # Docker 镜像构建文件
├── nginx.conf           # Nginx 配置文件
├── deploy.sh            # 一键部署脚本
├── dist/                # 构建产物（容器内）
└── .env                 # 环境变量
```

---

## 🔒 安全配置建议

### 1. 修改默认 API Key

编辑 `.env` 文件，使用你自己的 Key：
```bash
cd /opt/gujian-zhishi
echo "VITE_ZHIPU_API_KEY=你的API Key" > .env
docker-compose up -d --build
```

### 2. 配置防火墙

```bash
# 只开放必要端口
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp  # HTTPS
sudo ufw allow 22/tcp   # SSH
sudo ufw enable
```

### 3. 定期备份

```bash
# 创建备份脚本
cat > /opt/backup.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/backup/gujian-zhishi"
mkdir -p $BACKUP_DIR
tar -czf $BACKUP_DIR/backup-$(date +%Y%m%d).tar.gz /opt/gujian-zhishi
# 保留最近 7 天备份
find $BACKUP_DIR -name "backup-*.tar.gz" -mtime +7 -delete
EOF
chmod +x /opt/backup.sh

# 添加定时任务
echo "0 2 * * * /opt/backup.sh" | crontab -
```

---

## 📞 技术支持

部署遇到问题？

1. **查看日志**: `docker-compose logs -f`
2. **检查状态**: `docker-compose ps`
3. **重启服务**: `docker-compose restart`
4. **完全重装**:
   ```bash
   cd /opt/gujian-zhishi
   docker-compose down
   rm -rf *
   # 重新执行部署脚本
   ```

---

## ✅ 部署检查清单

- [ ] 服务器已购买并启动
- [ ] 安全组/防火墙已开放 80 端口
- [ ] 已连接到服务器（SSH）
- [ ] 已运行部署脚本
- [ ] 浏览器能正常访问
- [ ] AI 识别功能正常
- [ ] 3D 展示功能正常

---

**🎉 部署完成后，你就可以通过 http://服务器IP 访问古建智识了！**
