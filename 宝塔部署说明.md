# 古建智识 - 宝塔面板部署说明

## 问题现象
自己上传 dist 文件夹后，网站显示空白或 404。

## 原因
Vite 构建的项目需要正确的路径配置和伪静态规则。

## 解决方案

### 方法 1：使用修复后的代码（推荐）

我已经修复了路径配置，现在 `base` 改为 `/`，更适合宝塔部署。

**步骤：**

1. **在宝塔终端执行：**
```bash
cd /www/wwwroot/gujian
git pull
npm install
npm run build
cp -r dist/* .
```

2. **配置伪静态：**
   - 宝塔面板 → 网站 → 设置 → 伪静态
   - 选择模板：`vue` 或 `react`
   - 或粘贴以下内容：

```
location / {
  try_files $uri $uri/ /index.html;
}
```

3. **重启 Nginx**
   - 宝塔首页 → Nginx → 重启

### 方法 2：直接下载预构建版本

如果构建失败，直接使用我构建好的版本：

1. 下载 `dist.zip` 并解压
2. 把里面的所有文件上传到网站根目录
3. 确保包含 `.htaccess` 文件（Apache 需要）

### 方法 3：检查文件结构

正确的文件结构应该是：
```
/www/wwwroot/gujian/
├── index.html          # 入口文件
├── assets/             # JS/CSS 资源
│   ├── index-xxx.js
│   ├── index-xxx.css
│   └── ...
├── images/             # 图片资源
├── data/               # 数据文件
└── .htaccess          # Apache 伪静态配置（重要！）
```

## 常见问题

### 1. 刷新页面 404
**解决：** 配置伪静态规则（见上面方法1步骤2）

### 2. 页面空白，控制台报错 404
**解决：** 资源路径错误，确保 vite.config.ts 中 `base: '/'`

### 3. 图片加载不出来
**解决：** 检查图片是否在 `public/images` 目录，构建后会自动复制到 dist

### 4. API 功能不能用
**解决：** 检查是否配置了 `VITE_ZHIPU_API_KEY` 环境变量

## 宝塔一键部署命令

在宝塔终端执行：
```bash
curl -fsSL https://raw.githubusercontent.com/daydayupup520/gujian-zhishi/master/deploy-bt.sh | bash
```

## 联系支持
如果还有问题，请提供：
1. 浏览器控制台的报错截图
2. 网站访问地址
3. 宝塔面板的伪静态配置截图
